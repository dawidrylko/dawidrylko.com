---
import type { ImageMetadata } from 'astro';
import 'katex/dist/katex.min.css';
import { Image } from 'astro:assets';
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description?: string;
  date: Date;
  featuredImg?: ImageMetadata;
  featuredImgAlt?: string;
}

const { title, description, date, featuredImg, featuredImgAlt } = Astro.props;
// Matches Gatsby's moment format "DD MMMM YYYY" (locale pl), e.g. "05 czerwca 2016".
const dateFormatted = date.toLocaleDateString('pl-PL', { year: 'numeric', month: 'long', day: '2-digit' });
---

<BaseLayout title={title} description={description}>
  <article>
    <header>
      <h1>{title}</h1>
      <p><time datetime={date.toISOString()}>{dateFormatted}</time></p>
      {featuredImg && <Image src={featuredImg} alt={featuredImgAlt ?? ''} />}
    </header>
    <slot />
  </article>
</BaseLayout>
